# Shelfed frontend

This frontend is built with Vite + React and uses a dev proxy for `/api` requests.

## Setup

```bash
npm install
npm run dev
```

Make sure the Django backend is already running on `http://127.0.0.1:8000`.
